<%= form_with(model: agent_motive) do |form| %>
  <% if agent_motive.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(agent_motive.errors.count, "error") %> prohibited this agent_motive from being saved:</h2>

      <ul>
        <% agent_motive.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mb-3" style="width: 500px">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group mb-3" style="width: 500px">
    <%= form.label :score, class: "form-label" %>
    <%= form.number_field :score, class: "form-control" %>
  </div>

  <div class="form-group mb-3" style="width: 500px">
    <%= form.submit "Submit", class: "btn btn-primary" %>
    <%= link_to "Cancel", "javascript:history.back()", class: "btn btn-danger" %>
  </div>
<% end %>
